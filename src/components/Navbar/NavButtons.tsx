import React from 'react'

export default function NavButtons(): React.ReactElement {
  return (
    <div
      style={{
        border: '1px solid black',
      }}>
      nav buttons
    </div>
  )
}
